#include "main.h"
/**
 * print_rev - entry point
 * @s: paramter of function
 */
void print_rev(char *s)
{
	int i, j, k, l;

	for (i = 0; s[i] != '\0'; i++)
		j++;
	l = j;
	for (k = l - 1; k >= 0; k--)
		_putchar(s[j]);
	_putchar('\n');

}
