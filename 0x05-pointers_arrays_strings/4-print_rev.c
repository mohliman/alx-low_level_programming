#include "main.h"
/**
 * print_rev - entry point
 * @s: paramter of function
 */
void print_rev(char *s)
{
	int i, j, k;
	char ch[];

	i = 0;
	j = 0;
	while (s[i] != '\0')
		ch[] = s[i];
		j += 1;
	for (k = ch[j]; k >= 0; k--)
		_putchar(ch[j]);
	_putchar('\n');

}
