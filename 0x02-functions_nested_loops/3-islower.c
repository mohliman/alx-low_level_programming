#include <stdio.h>
#include "main.h"
/**
 * _islower - entry point
 * @c: parameter of _islower function
 * Return: one or zero
 */
int _islower(int c)
{
	if (c == islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
